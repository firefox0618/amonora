# STATE.md

## Current phase
Phase 2 — AI operating layer

## Current status
Canonical human-facing documentation has been created:
- PROJECT_OVERVIEW
- ARCHITECTURE
- DOMAIN
- REPO_RULES
- RUNBOOK
- FEATURES
- TEAM_CONTEXT
- PUBLIC_SURFACES

Initial AI operating layer has been introduced.
Initial project task workflow is active:
- April 12, 2026: `dashboard/ui` now carries the safe functional subset from `origin/ruslan` on top of the current design system: users screen got one-click operator slices (`Все / Активные / Paid / Trial / Expired / Blocked / Без доступа`) without removing existing dropdown filters, and campaign analytics now supports backend-driven date windows (`7d / 30d / this_month / last_month / custom date_from/date_to`) for both list metrics and campaign detail funnel.
- April 12, 2026: the tokenized client subscription page and the dashboard users table now share one device-count truth: public summary payloads expose `account_devices` / `account_devices_count` alongside legacy `bound_devices`, `client.amonoraconnect.com` renders the full account occupancy instead of only unified-link slots, and campaign analytics summary payloads now carry aggregate `trial_started / key_issued / renewed` totals so the operator analytics screen can mirror the imported KPI layout without inventing client-side counters.
- April 12, 2026: `dashboard/ui` user detail no longer treats legacy `vpn_clients` as the whole device truth: the panel now also serializes bound devices from the unified public-subscription surface, groups them by slot as view-only `Единая ссылка` cards, and shows the user's current `client.amonoraconnect.com/<token>` link with operator copy/access hints directly in the profile block.
- April 11, 2026: Estonia is no longer only a retired infra assumption in the active runtime: `185.88.37.71` now serves a fresh `x-ui`-backed `VLESS + Reality + TCP + xtls-rprx-vision` node on `est.amonoraconnect.com`, the region stays hidden from normal user country selection, but unified public-subscription feeds now provision and advertise `🇪🇪 #1 Эстония` alongside Germany and Denmark through dedicated `XUI_URL_EE / VPN_HOST_EE` settings.
- April 11, 2026: `@amonora_bot` now reuses the same inline/media `v2` UX router that was previously isolated in `@amonora_v_2_0_bot`: the main bot `/start` flow now follows `agreement -> channel-gated trial -> main menu`, existing active users land straight in the new menu, old reply-keyboard entrypoints are soft-redirected into the new screens, and the main bot now exposes the same `Моя подписка / Продлить / Поддержка / Информация / Бонусная система` flow on top of the already-shared payment/public-subscription seams.
- April 11, 2026: promo-code and gift-subscription MVP is now in repo code: shared promo models/schema live in `backend.core`, `dashboard/ui` has a new operator tab `Промокоды` plus create-code generator, and the `v2` bonus flow accepts promo/gift redemption and can sell gift subscriptions through the same `СБП / ручная СБП / крипта` payment seams.
- April 11, 2026: the imported campaign-analytics prototype is now integrated into the real Amonora operator stack instead of living as a standalone mini-app: `dashboard/ui` has a new tab `Аналитика`, backend v2 endpoints can create tracked offer campaigns from `channel_content_items`, old `/dashboard/analytics` now redirects to `/analytics`, and operators can view per-token summary plus a quick funnel `link_touched -> bot_start -> trial_started -> config_issued -> payment_success -> subscription_renewed` sourced from `analytics_daily_stage_counts`.
- April 10, 2026: confirmed-payment expiry drift now has a safe repo-side repair path in `ops/repair_subscription_expiry_drift.py`; the repair pass trims historically duplicated reconcile extensions back to the payment-backed expiry, re-syncs VPN/public-subscription access, and the isolated `@amonora_v_2_0_bot` subscription summary now shows an explicit `Ручное продление: на ...` line when dashboard-manual days exist on top of a paid tariff.
- April 10, 2026: the isolated `@amonora_v_2_0_bot` `/start` flow now branches by existing user state instead of always replaying the trial intro: brand-new users still see the agreement -> trial onboarding, returning users with active access go straight to the main menu, and users with an already used trial but no active subscription land on an explicit buy-subscription screen instead of the free-trial pitch.
- April 10, 2026: the isolated `@amonora_v_2_0_bot` now persists trial activation on `Проверить подписку` through the shared `get_or_create_user(...) -> activate_trial(...)` path, so the `agreement -> subscribe -> 3-day trial` sequence is truly one-time per user and repeated `/start` returns to the menu instead of replaying the onboarding copy.
- April 10, 2026: the isolated `@amonora_v_2_0_bot` no longer uses payment placeholders on the main monetization paths: `Продлить`, `Пополнить баланс`, and `Купить дополнительный слот` now create real Platega/manual payment intents through the shared payment layer, while the test-bot media mapping now reuses the sakura `Моя подписка` art for `Ключ` / `Скопировать ключ` / `Мои устройства` and uses the user-supplied sakura `Информация` image for the info screen.
- April 10, 2026: outward user-facing branding is now normalized to `Amonora`; active bot, landing, client-page, dashboard auth shells, and public legal/manual copy should stop using `Amonora Connect` / `Amonora VPN` in visible UI text, while domains and compatibility hosts remain `amonoraconnect.com`.
- April 5, 2026: Estonia is now treated as an infra-only host in active repo/runtime truth: `ee` is a retired product region, legacy `/vpn/activate` is reduced to `410 Gone`, test-bot Estonia profiles are removed, and new product provisioning is limited to Germany `de` and Denmark `dk`.
- April 5, 2026: the full `S2-13` repo-side reconcile pass is now in code: `ops.vpn_regions.reconcile_vpn_clients()` works as a dry-run/apply runner for Germany `xui`, Denmark `xray_core`, and retired Estonia cleanup, detects `local missing remote / remote orphan / expiry drift / metadata drift / retired EE cleanup`, and emits machine-readable result states plus follow-up users for legacy EE retirement.
- April 5, 2026: Grafana repo/runtime topology is now Estonia-side by design: canonical operator entry is `https://grafana.amonoraconnect.com`, `grafana-server` depends on `amonora-grafana-db-tunnel.service`, core `/grafana/*` is redirect-only, PostgreSQL stays on core, and analytics refresh remains on core through `amonora-analytics-refresh.timer`.
- April 5, 2026: the request-trace and provisioning-compensation follow-up is now in code: landing now seeds a shared trace context via `X-Request-ID`, control events prefer semantic `trace_id` values over accidental numeric DB ids, `device_compensation_jobs` now persist `request_id`, and bot/dashboard create-device failures queue a durable `finalize_created_device` recovery path instead of relying only on best-effort cleanup.
- April 5, 2026: confirmed-payment reconcile now keeps a canonical end-to-end `reconcile_state` in `payment_records.metadata_json`, finance sync feeds that same state, and `reconcile_confirmed_payment_records()` converges `effect -> access -> finance` in one recovery pass instead of three loosely-coupled candidate lists.
- April 5, 2026: dashboard user deletion now writes a durable `user_deletion_jobs` journal with `stage/status/payload/error`, so partial delete/rollback outcomes no longer disappear into transient exceptions alone.
- April 5, 2026: `ops.vpn_regions` now normalizes canonical region/runtime/provider/anti-sharing metadata for non-panel runtimes too; remote recreation stays limited to `xui` clients, but Denmark/Estonia device metadata no longer stay permanently stale just because they are not panel-managed.
- April 5, 2026: the production backup/restore rollout is now fully live on core: `amonora-core-pg-backup.timer` and `amonora-restore-validation.timer` are installed on the host, `backup.env` is sourced from the live app DB settings, the first server-side PostgreSQL dump succeeded, and restore-readiness now has a real `restore-proof.json` produced through a temporary-database drill using postgres-socket wrapper scripts when the app DB user lacks `CREATEDB`.
- April 6, 2026: the strict Grafana/business-analytics pass is now in code: operator dashboards are generated as a compact Russian thematic suite, `Главная Amonora` is now only a short overview, `Воронка роста` uses `Воронка подключения` vocabulary instead of UI-side `onboarding`, SQL stays backward-compatible by reading `onboarding_* OR connection_*`, and the payment layer now explicitly separates `subscription_activated` from `subscription_renewed` plus `payment_kind = new / renewal / other / unknown`, so first paid activations, real renewals, `Выручка новых`, `Выручка продлений`, and `Общая выручка` no longer depend on one mixed metric.
- April 6, 2026: channel-source attribution no longer requires a pre-created `channel_content_items` row for every `post_<token>` deep link: when `@amonora_bot` receives a valid `post_<token>` start payload without a matching channel content item, the runtime now still stores `channel_post` attribution plus a deduped `link_touched` analytics event under the normalized token instead of collapsing the visit into `organic_bot`.
- April 6, 2026: analytics attribution backfill now also reconstructs fallback `post_<token>` sources from `link_touched` / `bot_start` events, so `--backfill --full-refresh` no longer clobbers old users back to pure `organic_bot` after they revisit the bot through a tracked deep link; for returning users this preserves `first_source = organic_bot` while correctly updating `last_source` to the newest fallback token.
- April 7, 2026: `dashboard/ui` user detail now supports an explicit `Обновить` path that bypasses the normal `15s` detail cache, each device card exposes a separate human `Режим` label, and operators can run a per-device `Статус` check that returns an intentionally simplified server-side key-health verdict `Исправен / Сломан` with reason and checked time instead of overclaiming live client-side internet reachability.
- April 7, 2026: analytics event capture no longer waits only for the 10-minute `amonora-analytics-refresh.timer` to become visible in Grafana; freshly written growth/payment/connection events now debounce-spawn a background incremental `ops.analytics_refresh` pass, so new tracked `post_<token>` clicks and `/start` events surface in `analytics_daily_*` within seconds instead of only on the next timer tick.
- April 7, 2026: growth attribution rollups now preserve event-time attribution instead of blindly reusing the user's newest `last_source` for every historical event: new analytics events store captured `first/last` attribution snapshots in `payload_json`, while legacy rows fall back to a safer `seen_at` gate, so a newly clicked tracked link no longer inflates its Grafana `Старт / Оплата / Выручка` slices by stealing older events from the same user.
- April 7, 2026: the March/April gifted-months promo is now disabled repo-wide ahead of the date gate, so tariff durations are back to canonical `30 / 90 / 180 / 365` days and buy/reminder copy no longer mentions bonus months; referral rewards are also simplified to `50 ₽` by default and `100 ₽` for `12m`.
- April 8, 2026: the guarded `n8n` operator route on `https://amonoraconnect.com/n8n/` is restored again as an explicit nginx path to local upstream `127.0.0.1:5678`, and the legacy rollback workflow `amonora_daily_news_approval` is expected to stay disabled in live runtime so credentials drift there does not block the healthy core channel workflows.
- April 9, 2026: the isolated `@amonora_v_2_0_bot` test flow now has dedicated `Документы`, `Промокод`, `Подарочная подписка`, `Продлить`, `Пополнить баланс`, `Ключ` and `Мои устройства` screens with separate screen assets and safe placeholder-only payment actions, while the main `@amonora_bot` runtime remains untouched.
- April 9, 2026: the same isolated `@amonora_v_2_0_bot` flow now renders a compact per-device detail card from `Мои устройства` with model/OS/version fields plus a real `Удалить устройство` action that refreshes the device list after remote/local deletion, again without changing the main bot runtime.
- April 9, 2026: Amonora now has a repo-side public subscription contour at `client.amonoraconnect.com` with a canonical one-link contract: `/<token>` is now both the browser page URL and the Happ import URL, `landing` returns HTML to browsers but client-aware feed payloads to Happ on the same route, `/sub/<token>` remains only as a compatibility feed seam, and the server now binds Happ imports to slots `1..3`, stores `модель / ОС / версия ОС` from request metadata, and refuses a fourth unique device instead of issuing another working bundle.
- April 9, 2026: during the `@amonora_v_2_0_bot` phase, the unified Happ/public-subscription surface now exposes the Telegram action as the test bot URL (`https://t.me/amonora_v_2_0_bot`) and the Happ announce text was narrowed to a short instruction that points users to the `!` button for the site and the Telegram button for the bot.
- April 9, 2026: the public subscription follow-up now serves a cleaner client SPA that emphasizes `Telegram ID`, shows `трафик / ∞` plus bound-device count/list instead of the old `Серверов / Протоколы` blocks, exposes a top `QR для Happ + скопировать ссылку` action, and rewrites Happ route labels down to plain `#<slot> <country>` names; landing now also uses a raw-header path for the feed `announce` field so the unified feed can attempt a Russian `Все самое лучшее для Вас 😊` message instead of the previous ASCII translit.
- April 10, 2026: the test-bot unified-link flow no longer depends on `happ.lavivas.org`: `landing` now exposes an Amonora-owned wrapper route `client.amonoraconnect.com/happ/add?sub=...` that validates only local public-subscription URLs, auto-opens `happ://add/...`, and leaves HTTPS fallback actions (`Открыть Happ / Скопировать ссылку / Открыть страницу подписки`) if Telegram/web cannot launch the deep link directly.
- April 9, 2026: the unified public-subscription feed no longer uses the old raw per-route labels as its user-facing truth: `bot.public_subscription` now builds a normalized server list from canonical Germany/Denmark routes, client page summary can show the same human-readable names as Happ, and the user-level device cap for one-link imports now follows the real effective device limit instead of the original hardcoded `3`. The current user-facing unified-link surface is intentionally narrowed to the two primary routes `#1 Германия` and `#1 Дания`; reserve / experimental variants are not advertised until a separate runtime rollout is explicitly approved.
- April 10, 2026: the public client install-flow is now platform-specific instead of routing desktop users into a generic manual page: summary payloads on `client.amonoraconnect.com` now expose `Android / iOS / Windows / macOS / Linux / Apple TV / Android TV` entries with short per-platform install copy and direct official Happ sources (`Google Play`, `App Store`, `GitHub releases`, `APK`) that the SPA renders inside the expanded `Установка` block.
- `002` test inventory and risk map is complete
- `003` dashboard boundary mapping is complete
- `004` legacy UI vs panel UI coverage audit is complete
- `005` production runtime inventory is complete
- `006` backup verification pass is complete
- `007` restore-readiness pass is complete
- `008` backup governance and retention mapping is complete
- `009` off-host protection verification is complete at the server-evidence level
- `010` dashboard auth/session smoke hardening is complete
- `012` payment finalization contract tests are complete
- `013` payment confirmation idempotency tests are complete
- `014` dashboard API v2 contract smoke is complete
- `015` dashboard API v2 users contract smoke is complete
- `016` dashboard API v2 support contract smoke is complete
- `018` dashboard API v2 servers contract smoke is complete
- `019` dashboard API v2 traffic contract smoke is complete
- `020` dashboard API v2 settings contract smoke is complete
- `021` off-host backup production planning is complete
- `022` core PostgreSQL minimal off-host backup rollout is complete
- `023` repeatable manual core off-host backup script is complete
- `024` daily local automation for core off-host PostgreSQL backup is complete
- `025` local retention cleanup for core off-host PostgreSQL backups is complete
- `026` local cloud-upload preparation for core PostgreSQL backups is complete
- `027` local VPN-node `3x-ui` backup copies are complete
- `028` daily local automation for VPN-node `3x-ui` backups is complete
- `029` local restore drill is complete
- `030` one-click restore script is complete
- `031` VPN access flow map and hardening plan is complete
- `032` device region change hardening is complete
- `033` VPN sync failed -> repair-needed state is complete
- `034` VPN repair-needed visibility in panel UI is complete
- `035` VPN repair action (manual re-sync) is complete
- `036` panel UI frontend check pass is complete
- `037` VPN repair event log is complete
- `038` alerts / attention surface mapping is complete
- `039` overview attention rail for user-level repair issues is complete
- `040` minimal system ops alerts surface is complete
- `041` unified overview attention section is complete
- `042` payment / support / access triage mapping is complete
- `043` manual payments hardening mapping is complete
- `044` support backlog and repair workload mapping is complete
- `045` post-payment access verification hardening is complete
- `046` payment-related issues in overview attention are complete
- `047` manual payment queue aging and actionability is complete
- `048` payment/support/user linkage hardening is complete
- `050` backup visibility in dashboard is complete
- `059` lightweight issue prioritization layer is complete
- `060` minimal auto-escalation (no notifications, no infra) is complete
- `061` operator workflow shortcuts (fast actions) is complete
- `062` safe batch repair (very limited) is complete
- `063` guardrails for dangerous actions is complete
- `064` operator session clarity (what just happened) is complete
- `065` repair reason normalization is complete
- `066` repair source / outcome normalization is complete
- `105` Denmark standalone `Xray core` node rollout is complete at the ops/runtime level
- `106` Denmark provider-based product integration is complete as a controlled rollout layer
- `108` Denmark golden-node hardening and safe fleet baseline is complete
- `107` cosmic/Web3 public landing redesign is complete
- `109` bot balance/support-media/key-wording hardening is complete
- `110` Amonora Control bot rollout is complete
- `111` referral balance backfill, panel UI unification, support media restriction, and WireGuard cleanup are complete
- `112` bank-facing public legal surfaces now include owner/support contacts, updated privacy/terms/refunds pages, and a dedicated public contacts page without risky VPN marketing wording
- `113` main `@amonora_bot` surfaces now use the redesigned home/menu/info/support/devices/tariff shell from `Дизайн.txt`, while keeping access/payment/device-domain logic unchanged
- `114` `@amonora_control_bot` now uses the full redesigned shell from `Дизайн бота контроль.txt`, including role-aware settings, owner-only broadcasts/triggers, per-admin notification preferences, campaign/template storage, and a 5-minute trigger worker tied to `users.last_activity_at`
- `117` control-bot post-redesign UX cleanup is complete: compact `/start`, shorter separators, Telegram-first payment review, compact auth sessions, short node/manual-payment notifications, and correct Sweden/unknown region mapping for node counters
- `115` landing now exposes the user guide at hidden direct-link route `/manual`, while keeping `documentation/supporting/user-guide.md` as the single source and leaving the main public navigation unchanged
- `116` `x-ui`-backed device keys now apply a single-IP limit on create/sync/reissue paths, reducing one-key multi-device reuse without changing the Denmark `xray_core` seam
- `118` public terms now explicitly prohibit using the service to access legally restricted resources, and new `@amonora_bot` users now see a single first-start trial message with offer-acceptance wording and a direct link to `/legal/terms`
- the public offer now also fixes that third-party resources are chosen by the user independently, broad overreach language about total immunity is intentionally avoided, and the service reserves the right to limit access on complaints, abuse signals, or lawful requirements
- first-start `/start` carries the offer-acceptance wording and direct offer link, while repeated `/start` with active access stays on the shorter existing status message
- `119` temporary referral placeholder is no longer current state: `@amonora_bot` again exposes a live referral screen with personal `ref_code` links, balance/earned counters, level progress, and tariff-based rewards for the first confirmed payment of an invited user
- the March 26 referral notifications pass is complete: referral reward accrual now writes a dedicated control event, both sides receive deduped bot/in-app notifications only after balance credit on the first confirmed payment, referral share copy is multiline, and an optional push webhook can mirror the same event when connected
- `120` the main `@amonora_bot` now uses the country-first connection UX, and the earlier friendly-mode layer has since been simplified further into `Стабильный / Мобильный / Резерв` while internal `vless` / `trojan` identifiers and provisioning paths stay hidden from normal users
- `121` `dashboard/ui` now includes a dedicated `Audit log` surface, EKB role/time topbar context, and safer support quick actions bound to the real internal user profile instead of the support-ticket identifier alone
- `122` the old backend-rendered dashboard page shell is now retired from active use: legacy `GET /dashboard/*` routes redirect into the new `dashboard/ui`, and the new `Amonora Control` shell/auth/overview visual layer is now the primary operator surface
- `123` control-center ops completion is complete: role-aware route/data boundaries, `sync` / `deep repair`, expanded payment status flow, server actions, finance gating, and 24h traffic/dashboard surfaces are now wired into the active `dashboard/ui`
- `124` the control-center product specification has been refreshed in Russian as `amonora_control_tz_v5.md`, separating mandatory production scope from UX+ polish requirements
- `126` `@amonora_control_bot` now matches the v3 operational spec much more closely: new `Dashboard / Problems / Support / Notifications / /user` surfaces, 5-minute panel login codes, support actions wired through the same backend seams as the dashboard, node quick-actions, and per-role notification defaults with `support/security` buckets
- `127` the second safe ecosystem reliability pass is complete: duplicate Telegram Stars updates no longer replay subscription finalization, balance-aware payment creation now serializes on the user row, support replies reject missing local tickets before Telegram delivery, and successful device provisioning now has a safe recovery path when credential delivery fails
- `130` the 5-minute control worker now also emits deduped node/service/user incident alerts with recovery handling, without adding a new runtime service
- `140` control-bot channel post button editing is complete: owner/admin users can forward an existing channel post into `@amonora_control_bot`, send URL buttons in `Текст | URL` format, and update or clear the original post keyboard in place
- `131` dashboard regression pass is complete: topbar compacted and split into title/actions + lower time/status row, notification dismiss-state persists across reloads, payments page hardens against partial finance payloads, support media previews now expose direct-open links, audit switched to a compact Russian table layout, and user IP fields no longer fall back to hostnames
- `134` mobile test-profile rotation is complete as a controlled admin-only surface: `@test_amonora_bot` now exposes 8 new mobile-oriented profiles across Germany, Denmark, and repurposed Estonia test infrastructure, while `@amonora_bot` and its production user routes remain unchanged
- the March 26 Denmark Telegram-proxy pass is complete: Denmark now also carries a separate Telegram-only `MTProto` proxy surface on `11443/tcp`, kept intentionally outside the normal `Xray` `443/8443` VPN profile family and distributed only as a manually shared `tg://proxy` link
- on March 29, 2026 the Denmark Telegram proxy runtime was migrated from `mtg v2` to the official `Telegram MTProxy` implementation because `@MTProxybot` requires a classic 16-byte hex secret and `mtg v2` removed `adtag` support; the live service is now `amonora-dk-mtproxy.service`, while the old `amonora-dk-mtg.service` is disabled as rollback-only evidence
- Estonia is now also approved as a safe fallback public web-edge host: if the current core-host web IP proves unstable for users without VPN, `185.88.37.71` may terminate `amonoraconnect.com` / `www.amonoraconnect.com` at `nginx` and reverse-proxy all public web traffic back to core `46.21.81.186`, changing only the public site IP while keeping backend, bots, PostgreSQL, and dashboard runtime on the core host
- the March 29 Estonia runtime migration is complete at the node-ops layer: `185.88.37.71` no longer runs `docker + 3x-ui + xray`, now exposes only `SSH + AmneziaWG` (`awg-quick@awg0`) on `51820/udp` with subnet `10.10.8.1/24`, and core-side tunnel units `amonora-xui-tunnel-ee.service` / `amonora-xui-ee-tunnel.service` are disabled
- after this ops-only migration, any remaining code/docs assumptions about Estonia `XUI_URL_EE`, hidden `EE` activation via `3x-ui`, or test-bot mobile profiles on `443/8443` should be treated as stale follow-up work rather than current runtime truth
- test-bot now also has a repo-side `AmneziaWG` delivery path for Estonia test configs: instead of storing AWG private material in git, it reads a server-side config file from `AMONORA_TEST_BOT_AWG_PHONE_CONFIG_PATH` and can encode that full config into QR/text for admin-only testing
- the March 29 core automation rollout is complete at the runtime layer: core host `46.21.81.186` now also carries an `n8n` service `amonora-n8n.service` on local upstream `127.0.0.1:5678`, installed in isolated path `/opt/n8n` with explicit `512M` / `50% CPU` limits; as of April 1, 2026 it also has a guarded operator HTTPS entry at `https://amonoraconnect.com/n8n/` through the existing `nginx` vhost, protected by outer basic auth while the upstream itself remains local-only
- the March 31 channel automation MVP is complete: PostgreSQL now carries `channel_content_items` plus `channel_post_touches`, `dashboard` exposes shared-secret internal generate/publish endpoints for `n8n`, `@amonora_control_bot` has owner/admin `/channel` review/edit/approve/publish-now screens, OpenAI channel drafts are safety-checked before approval, and `@amonora_bot` now understands `post_<token>` deep links for separate channel attribution through trial/payment conversion
- the March 30 mobile/admin follow-up is complete: admin-only `Мобильный` mode can now deliver operator-defined shared links for Germany and Denmark through env overrides, while Estonia monitoring no longer derives runtime health from retired `3x-ui/xray` signals and local core node degradation noise is suppressed in favor of real `down` or service-level incidents only
- the April 2 mobile-route follow-up is complete: `Мобильный` mode is now user-facing for Germany and Denmark, still delivered through runtime shared links from env overrides, while product copy now describes it as a route for networks where only part of directions remains available
- the April 2 dashboard support reply hotfix is complete: when Telegram rejects delivery because the user blocked `@amonora_support_bot` or another send error occurs, the backend now returns a structured business error to `dashboard/ui` instead of leaking an HTML 500 traceback into the control-center support screen
- the April 2 manual-SBP reminder pass is complete: `dashboard/ui` payments detail now exposes a dedicated reminder action for open `sbp_manual` records, and the backend sends the user a targeted follow-up that points them to support for requisites or to direct cancellation if they are no longer going to pay
- the April 2 payments follow-up is complete: the `dashboard/ui` `v2` payments payload now serializes `can_send_reminder` too, so the reminder button actually appears for eligible open manual `СБП` records like `#141` instead of being silently hidden by payload drift
- the April 2 support-formatting follow-up is complete: `dashboard/ui` support history now preserves multiline message formatting with visible line breaks, and the operator reply box is a real multiline textarea where `Enter` inserts a new line instead of forcing single-line drafting
- the April 2 trial-subscription enforcement pass is complete: first-time trial still starts only after channel membership, but active trial now also depends on staying subscribed; if the user unsubscribes before `trial_expires_at`, the trial is paused and VPN access is revoked, a one-time explanatory Telegram notice is sent or backfilled, and after resubscription the same trial resumes only with the remaining time instead of issuing a new trial
- the April 3 n8n channel-runtime repair is complete at the production layer: core host `n8n` now again contains the repo-managed channel-MVP workflows `generate_due_channel_drafts`, `publish_approved_channel_posts`, and `remind_missing_channel_content` as active schedules
- the April 3 daily-news automation follow-up is now complete repo-side and ready for production rollout: the experimental `amonora_daily_news_*` path uses PostgreSQL-backed internal dashboard endpoints instead of `Google Sheets`, `amonora_daily_news_generate` auto-publishes generated posts through a new internal publish seam, and when a slot has no fresh relevant news it emits a fallback evergreen post so the daily schedule does not stay empty
- the April 3 paid device-slot add-on pass is complete repo-side: normal paid users can buy `+1 device` add-ons for `49 ₽` up to an effective max `8`, add-ons live only until the end of the current paid period via `DeviceSlotEntitlement`, payment finalization distinguishes `device_slot_addon` from subscription activation, `@amonora_bot` now upsells an add-on instead of a hard stop on device-limit reach, `dashboard/ui` and `Amonora Control` show dynamic `devices/max_devices`, and the 5-minute worker expires stale add-ons without auto-deleting existing devices
- the April 3 dashboard/payments/support hardening pass is now in code: payment records now dedupe on `(payment_method, external_payment_id)`, payment-effect application uses an explicit claim/apply guard to reduce duplicate callback/manual races, balance-only spends and paid-subscription extension paths lock the user row, manual payment mutations and `.env` edits are owner-only in the dashboard API, audit IP now prefers trusted proxy headers from the local reverse-proxy seam, and support storage no longer auto-trims long ticket history or auto-deletes closed tickets just because they became old
- the April 3 payment-intent follow-up is now in code: `@amonora_bot` reuses an already open payment intent for the same semantic product across `manual / Platega` method switches instead of creating a second active invoice/request for the same tariff, device-slot add-on, or balance top-up amount
- the April 3 dashboard operator-audit follow-up is now in code: additional dangerous seams (`create/delete payment`, provider sync, manual-payment reminder, `create/delete device`, `delete user`, and `support reply`) now persist richer `before / after` audit payloads instead of short opaque strings
- the March 23 finance/support/start-flow pass is complete: payments now have users-style filters and manual refresh, payment review buttons lock on terminal states, support attachments open through the Next proxy path instead of the legacy `/dashboard/support/*` delivery seam, and first-time trial activation now depends on channel subscription: subscribed users get trial on `/start`, while unsubscribed users are sent to the channel and can confirm membership through the in-flow `Подписался` button without retrying `/start`
- the March 23 landing bridge-access pass is complete: the public landing now exposes a free 24-hour temporary-key flow at `/bridge/access`, returns the user to `@amonora_bot` for trial/payment continuation, shows a contextual `/manual` button after key issuance, and hides synthetic `bridge_*` users from normal dashboard aggregate slices
- the March 24 Platega auto-payments pass is complete: `@amonora_bot` now creates auto `СБП` and crypto payments through `Platega`, `landing` accepts the provider callback at `/webhooks/platega/{secret}`, `dashboard/ui` exposes provider status plus manual `sync` for auto-payments, and manual `СБП/крипта` remain only as hidden emergency rollback paths
- the March 24 hardening pass is complete for the previously logged P1/P2/P3 findings: payments/finance cache keys are now admin-scoped, trial issuance is guarded in the backend layer, Denmark host fallback no longer drifts into the global host, landing bridge access is rate-limited at both app and edge layers, legal/manual pages now expose the same cookie-consent surface as the homepage, operator health now watches `nginx` plus critical timers, overview 30-day revenue uses confirmation time, and the legacy `Crypto Pay` webhook is disabled by default as a rollback-only seam
- the March 24 Estonia alert-noise suppression pass is complete: `ops/server_watchdog.py` and `ops/control_error_triggers.py` now mute `ee` node notifications so `Amonora Estonia Node` no longer clutters control-bot alerts while panel-side node visibility remains unchanged
- the March 24 payment/control follow-up pass is complete: `dashboard/ui` now auto-refreshes open `Platega` payments without relying only on manual provider sync, `@amonora_bot` has a separate `Пополнить баланс` flow through `Platega`, user block/unblock/extend/payment actions now push Telegram updates with refreshed menus, user detail now exposes running balance history, and control notification preferences now support finer toggles for `new users / trial start / key issuance`
- the March 24 operator dashboard cleanup pass is complete: `/manual` mobile readability has a stronger fallback, overview tariff slice is now fixed in `1 / 3 / 6 / 12 месяцев` order and rendered in a single operator column, audit labels now localize `request_login_code_v2 / remove_user_tariff / server_watchdog_overloaded`, finance/payment revenue now counts only confirmed `Platega` `СБП + crypto` methods, and traffic baseline now auto-resets on the first day of the month through the active watchdog/runtime seam
- `100–102` frontend chart/form quality fixes across panel UI and legacy `dashboard` are complete
- the March 21 user-facing content pass is complete for device-region labels, landing copy/mobile hero behavior, and active admin naming (`Панель управления`)
- `011` repo cleanup safe items is complete
- `017` dashboard `datetime.utcnow()` deprecation cleanup is complete

## Confirmed realities
- `dashboard` remains operationally important
- `dashboard/ui` is the current main UI layer of the Панель управления
- backend-rendered legacy dashboard pages are no longer the intended operator entry surface; `/dashboard/*` page-routes now act as compatibility redirects into the root admin UI
- the active control center now treats backend role checks as the source of truth for both read and action access, not only for button-level UI state
- core logic is still partially distributed
- runtime truth must still be verified where docs are based on repository inference
- a first live production runtime inventory has now been completed in read-only mode
- backup reality has been partially verified from live servers, but backup governance is still incomplete
- restore readiness has been partially mapped, but remains fragile and assumption-heavy
- backup governance and retention posture has now been mapped, with clear evidence of operator-memory risk
- off-host protection is not confirmed for any of the three known production hosts
- provider panel review shows no backup tab on `46.21.81.186` and `185.88.37.71`, and a paid-but-inactive backup feature on `213.108.20.34`
- current test surface is real but small, with many critical flows protected mostly by smoke scripts rather than mature automation
- the first auth/session hardening pass now protects the narrow backend/session seam shared by legacy and v2 dashboard flows
- payment finalization now has a focused contract test seam, but idempotency still lives outside the finalization orchestrator itself
- `confirm_external_payment_record` is now explicitly protected as the current idempotency boundary, while duplicate-path `note` mutation remains part of the documented contract
- Telegram Stars duplicate delivery no longer replays the finalization orchestrator on top of the existing `confirm_external_payment_record` idempotency seam
- `dashboard/ui` now has a first contract-smoke layer on top of `overview` and `payments`, covering both auth-sensitive access and minimal load-bearing response shape
- the load-bearing `users` list contract for `dashboard/ui` is now also protected at the top-level shape boundary
- the load-bearing `support` queue contract for `dashboard/ui` is now also protected at the top-level shape boundary
- the load-bearing `servers` contract for `dashboard/ui` is now also protected at the top-level shape boundary
- the load-bearing `traffic` contract for `dashboard/ui` is now also protected at the top-level shape boundary
- the read-only `settings` contract for `dashboard/ui` is now also protected at the top-level shape boundary
- a first safe repo-cleanup pass has removed local Python cache clutter and tightened ignore rules without touching runtime code or legacy surfaces
- the auth/security helper no longer uses deprecated `datetime.utcnow()` on the tested code path
- a baseline diff audit against `origin/codex/server-baseline-20260319` has now been captured, separating safe, review-needed, and runtime-sensitive divergence zones
- the next stage backlog is now explicitly captured so the project can continue through small controlled hardening steps instead of broad refactors
- the first production-oriented planning pass now recommends external object storage as the primary off-host backup strategy, with provider snapshots only as a possible later supplement
- the first real off-host PostgreSQL dump copy from the core host has now been executed and verified on a local off-host destination
- the first repeatable manual operator command for core off-host PostgreSQL backup now exists
- the first narrow daily automation for the same core off-host PostgreSQL backup path now exists on the local machine
- the local core off-host PostgreSQL backup flow now includes basic 7-day retention cleanup for `core-pg-*` folders
- the local core off-host PostgreSQL backup flow is now prepared for optional `rclone` cloud upload, but cloud protection is not yet active until a remote is configured
- local off-host copies now also exist for Germany and Estonia `3x-ui` artifacts
- daily local automation now exists for the VPN-node `3x-ui` backup path as well
- local restore drill now confirms that the current PostgreSQL dump and VPN `x-ui.db` artifacts are structurally usable, though full restore remains only partially verified
- a one-click local PostgreSQL restore drill script now exists and runs the Docker-based local import path end-to-end
- the real VPN access lifecycle is now mapped as a canonical current-state flow with explicit drift points and narrow hardening candidates
- existing-device cross-region change no longer silently rewrites region metadata without real reprovisioning
- payment-driven VPN sync failures now persist a repair-needed marker instead of disappearing at request end
- the repair-needed marker is now surfaced in `dashboard/ui` user detail for direct admin visibility
- admins can now trigger a narrow manual VPN re-sync from `dashboard/ui` user detail, with the repair marker clearing on success and staying visible on failure
- the `dashboard/ui` frontend typecheck/build path has now been re-confirmed after the VPN repair UI changes
- manual VPN repair attempts are now stored as a small event history and surfaced in `dashboard/ui` user detail
- current user-level and system-level attention signals are now mapped in one canonical document, including visibility gaps and suggested UI placement
- overview now surfaces a narrow user-level `Needs attention` rail for repair-related issues
- overview now also surfaces a minimal `System status` block for backup freshness, support backlog, and manual payment queue
- overview attention now reads as one coherent section instead of split user/system blocks
- payment/support/access manual triage hotspots are now mapped in one canonical document with next high-value hardening candidates
- the real manual-payments lifecycle is now mapped as a canonical current-state flow with explicit operator touchpoints and friction seams
- the real support backlog and repair workload are now mapped as a canonical current-state flow across payment, access, and ticketing seams
- post-payment finalization now explicitly distinguishes `access incomplete` vs `sync failed` instead of treating payment activation as the full end of the flow
- overview attention now explicitly distinguishes payment-related repair issues and stale pending payment confirmations
- overview payments status now surfaces the oldest pending manual payments with an explicit stale rule and direct payment links
- payment detail now exposes first-level user/access/support linkage instead of forcing blind cross-screen reconstruction
- dashboard overview backup status now bridges local backup health into UI with explicit stale threshold and per-source visibility
- fresh payment and manual-repair VPN sync failures now get one safe automatic retry with explicit auto-repair events and no looping
- dashboard overview now also exposes restore-validation freshness so backup visibility is no longer blind to restore confidence
- repair-needed reasons are now normalized around canonical `post_payment_*` and `manual_repair_*` names, with legacy aliases still readable in UI/grouping
- overview support backlog now includes a compact actionable slice of oldest open tickets instead of count-only visibility
- payment detail now includes a compact user issue summary for repair/access problems without forcing an immediate user-detail jump
- support detail in `dashboard/ui` now also carries a real linked-user context, so operator actions from a ticket run against the correct internal user record
- `dashboard/ui` now has a dedicated audit surface instead of exposing audit only as a small slice inside settings/overview
- `dashboard/ui` now exposes the final operator action set from the control-center brief: `sync`, `deep repair`, expanded payment statuses, and server `restart / health_check / maintenance / migrate`
- manager-role sessions now stay inside the payment/user/support operating slice while server/settings/finance read surfaces are backend-gated away
- overview now carries a lightweight explicit priority layer so operators can see which issues matter most right now
- overview now also visually escalates stale repair/payment/support issues based on simple timestamp thresholds, without adding notifications or background infra
- operator-facing overview/payment/support contexts now include faster one-click shortcuts and inline repair entry points without widening backend behavior
- overview now supports a very limited sequential batch repair flow on top of the existing single-user repair API
- repair actions now carry lightweight `can_repair / repair_block_reason` guardrails across overview, user detail, and payment-linked context
- repair actions now also use shared clearer post-action feedback so operators can immediately see whether a repair succeeded, failed, or was skipped
- repair reason payloads now consistently carry both canonical codes and human-readable labels across overview, user detail, payment context, and repair history
- repair payloads now also consistently separate `source`, `outcome`, and `reason`, so success/skipped events no longer masquerade as generic failures
- the main `dashboard/ui` Recharts pages now wait for real container dimensions before rendering, reducing width/height warnings during initial navigation and refresh
- visible form controls across both admin UIs now explicitly carry `id` / `name`, reducing browser-console accessibility warnings without changing submission semantics
- legacy spark charts now skip zero-size render attempts and retry on the next frame instead of drawing immediately into hidden containers
- a clean standalone Denmark node now exists at `dk.amonoraconnect.com` with `Xray core` + `VLESS / Reality / XHTTP`, and `x-ui` has been removed from that host
- the active VLESS provisioning seam is now provider-aware, so Denmark can be handled through standalone `Xray core` while Germany/Estonia stay on `XUIClient`
- Denmark now also has a documented golden profile with `www.apple.com` camouflage, primary `443` `XHTTP packet-up`, reserve `8443` `XHTTP packet-up`, explicit DoH upstreams (`cloudflare-dns.com`, `dns.sb`, `localhost`), `fingerprint = chrome`, and `loglevel = none`
- Germany and Estonia now share a safe fleet baseline without transport migration, including `bbr`, `tcp_fastopen = 3`, `tcp_mtu_probing = 1`, larger buffers, `nofile = 65535`, persistence-safe proxy sniffing through `x-ui.db`, shielded public panel ports, and persistent torrent blocking
- official client pack artifacts now exist for `v2rayNG`, `Nekoray`, and `Streisand`, with split/full-tunnel policy, JSON import artifacts, QR/onboarding references, and MTU guidance (`1400` default / `1420` fallback)
- the main bot can now deliver a device-matched `Маршруты РФ` split-routing JSON pack so Russian destinations stay direct while foreign traffic goes through VPN without changing the existing key flow
- Estonia is now hidden from normal user-facing region selection and remains reserve/testing infrastructure
- Denmark is now part of the normal user-facing region choice alongside Germany, while Estonia is retired from the product VPN contour and kept only for infra/legacy cleanup purposes
- explicit rollback evidence for the Denmark change now exists both on-host and in the local off-host backup location
- active user-facing copy now treats Germany and Denmark as the two main public routes, while Estonia is retired from product routing and remains only in infra/runtime context
- the public landing now uses a simpler client-facing message structure and a calmer mobile hero layout instead of the older internal-note-like wording
- the public landing now also has a cinematic cosmic/Web3 visual system with a CTA-first ultra-short structure, while keeping legal/cookie surfaces on the same runtime seam
- the public landing now also serves a hidden direct-link manual page at `/manual`, sourced from the existing user-guide markdown and intentionally absent from homepage navigation
- the public landing now also has a narrow bridge-access surface for Telegram-blocked users: it can issue a free 24-hour temporary key and then hand the user back to `@amonora_bot`, while the active landing UI keeps tariff selection in the Telegram-first flow instead of a separate web checkout
- bot payment flows now support an internal ruble `Баланс` with referral-credit migration, automatic reserve/apply/release semantics for RUB payments, and no mixing with `Telegram Stars`
- from `March 27, 2026` through `April 7, 2026` inclusive, tariff-side promo logic is date-gated instead of static: `3m / 6m / 12m` temporarily expand to `120 / 240 / 455` days, tariff/payment copy advertises the matching gifted months, and trial-conversion reminders/triggers use the same bounded promo window instead of an evergreen deadline string
- the March 28 Estonia activation MVP is now live on the core host: `POST /vpn/activate` accepts app-side activation requests for existing Estonia `EE` VPN keys, hashes the reported fingerprint, validates active access server-side, enforces the configured per-key device cap, and keeps the whole seam test-only without exposing Estonia in the normal public region picker
- user-facing bot copy now consistently says `получить ключ` for active VPN protocols
- `x-ui`-backed VLESS/Trojan keys now keep a single-IP panel limit through create/sync/reissue flows, while Denmark `xray_core` still needs a separate enforcement mechanism for equivalent behavior
- March 25 SBP incident hardening is now in place: if `Platega` callback is blocked on the public edge before reaching `landing`, `@amonora_bot` can force tariff-side `СБП` into the existing manual payment flow via `FORCE_MANUAL_SBP_USER_FLOW`, while hiding broken `СБП` balance-topup entrypoints
- support tickets now persist structured attachment metadata and both admin UIs can open support attachments through a backend route backed by Telegram `file_id`
- internal operational notifications now have a dedicated `Amonora Control` Telegram bot with a centralized `control_notification_events` log, while `Support Bot` is intentionally reduced to client tickets/media/assignment/replies only
- `Amonora Control` is no longer only a notifications console: it now also has owner-only admin/user messaging, template management, active session visibility, and DB-driven trigger delivery through the shared `amonora-access-reminders` worker
- the shared 5-minute `amonora-access-reminders` worker now also acts as a control-plane incident scanner for node degradation, core service failures, and aggregated user `vpn_repair_needed` issues
- node watchdog now treats `SSH monitoring gap + healthy runtime` as a non-paging monitoring blind spot instead of a full node outage; Germany also has a restored core-host `22/tcp` allowlist, while Estonia still shows an external `Connection refused` from the core host and stays visible only as a monitoring gap
- control-center access mutations now return structured sync outcomes, so `trial / extend / clear access / block` can surface partial node failure instead of false full success
- deep repair now performs a post-repair sync verification step and re-flips the user into `repair_needed` if the rebuilt state still does not converge
- VLESS and Trojan cleanup/sync flows now resolve missing `inbound_id` across available inbounds, reducing silent stale-key drift after delete/reissue/repair
- payment status/delete guards are now stricter around confirmed records, and balance-held non-confirmed records release reserved balance on terminal transitions
- node overall health now includes runtime-service degradation (`xray / 3x-ui`), so system health is less likely to stay false-green when VPN runtime is actually down
- settings now expose active `roles / notifications / integrations` read surfaces and distinguish `refresh status` from `restart`, with `.env` updates reporting `restart required`
- the traffic screen now explicitly labels the 24h event curve as control-plane/operational activity instead of presenting it as pure live network traffic
- support transfer now rejects unknown admins, and support close/reply flows now reflect Telegram-delivery uncertainty instead of always claiming guaranteed client notification
- the active shell now supports `Ctrl/Cmd + K` search focus and defers dashboard/global search input to reduce unnecessary query pressure during rapid typing
- support bot admin queue now renders only the latest 5 tickets per filter and no longer expands into long inline-keyboard lists
- the main bot now gates first-time trial on `@amonora_new` membership: already-subscribed users get trial directly on `/start`, while unsubscribed users are first sent to the channel and then confirm membership through the in-flow `Подписался` button, with the offer wording and channel button staying in the same first-start message
- the users control-center surface now carries a cached Telegram-channel subscription signal (`subscribed / not_subscribed / unknown`) with bounded Telegram API pressure
- task `128` originally captured the support-list cap, first-start trial onboarding, and cached `@amonora_new` membership visibility in the users surface; the current trial flow now depends on channel membership before activation
- task `129` captured split-routing pack delivery in the main bot and aligned the official client-pack artifacts to Xray-compatible routing rules
- dashboard-confirmed non-manual payments now pass through the same post-payment finalization orchestration as the main payment flow instead of a narrower direct-extend path
- support queue counters now use SQL aggregates, and control-bot node detail now exposes `Обновить статус` as an honest refresh action instead of a misleading `Resync`
- server migration is now intentionally conservative: devices without explicit source-node binding are skipped into manual review, and partial migrations no longer auto-mark the source node as `maintenance`
- task `131` captured the second safe ACR pass over payment finalization, support counts, and node action/migration semantics
- the March 23 control-center density pass is now live on the core host: ash-gray matte light/dark themes, tighter cards and sidebars, compact auth/topbar, overlay user/payment details, audit/roles/traffic-reset API seams, and successful `dashboard + dashboard-ui` restart with `200 /login` and `401` unauthenticated API smoke
- the destructive behavior of the standard `sync_user_access` action is removed: plain `Синхронизировать` now performs a soft access sync with one safe retry, while key/config reissue remains only under `Глубокий ремонт`
- the March 23 compact operator follow-up now adds: list-style sidebar, sticky compact topbar, denser overview/users/support layouts, tabular `servers / payments / traffic`, bottom-right glass toasts, local notification clearing, inline support media preview, and runtime role-permission overrides for `tech_admin` / `support_admin`
- the main bot now exposes `Стабильный / Мобильный / Резерв` instead of the older `Автовыбор / Нова / Ядро / Основа` layer; `Мобильный` remains a public placeholder for regular users, while admins keep an experimental Denmark-backed mobile seam for live testing, existing-device `Режим` still opens direct mode selection without re-asking for country, and complimentary admins/support-admins now have a `10-device` limit instead of the default `3`
- `/manual` on the landing no longer depends on legal-page reveal animation to become visible, and legal/manual text now uses stronger readable contrast on mobile instead of muted glass-only rendering
- `dashboard/ui` users now support explicit `1m / 3m / 6m / 12m` plan filtering and tariff-aware search, while the overview tariff slice keeps those operator buckets visible even when the current count is zero
- user detail in `dashboard/ui` now attempts live `3x-ui clientIps` lookup via the confirmed POST seam and falls back to stored metadata IP when the node still returns `No IP Record`; current Germany/Estonia runtime still needs a persistent panel-side access-log enable step before those live IP records become broadly populated
- anti-sharing is now configurable instead of hardcoded to `1`: `VPN_MAX_DEVICES_PER_KEY` drives `3x-ui limitIp`, while Denmark `xray_core` uses an expanded lease state with `N active IPs`, optional per-key whitelist file entries, and soft-limit violation telemetry
- dashboard user detail now emits a dedicated per-device `technical` block so the panel can show available OS/model/MAC/IP/provider/transport/anti-sharing fields without depending on raw metadata keys
- task `160` is now complete: smart trial funnel lives in the existing `bot + PostgreSQL + amonora-access-reminders + control triggers` contour, `users` now persist `trial_activity_level / trial_engaged_at`, technical engagement upgrades trial users from `low` to `active`, legacy `trial_ends_1d / trial_ends_today` rules are replaced by `2h / 24h / final-6h` segment-aware trigger rules, and trigger CTA now supports opening the `📱 Устройства` screen directly from funnel messages
- the April 3 hardening follow-up now adds three more protections: confirmed payments without `effect_applied_at` are periodically re-processed by `amonora-access-reminders`, dashboard login codes survive process restarts through PostgreSQL-backed storage instead of only in-memory `_PENDING_CODES`, and `dashboard/ui` knowledge HTML now strips dangerous raw tags/handlers instead of trusting markdown output as-is
- the April 3 second hardening follow-up is now in code: trial technical engagement now persists fresh `last_activity_at` updates even after a user is already in the `active` segment, device creation no longer upgrades trial users before config/QR delivery actually succeeds, cookie-auth same-origin checks now also protect both legacy and `v2` login/verify POST seams, dashboard `.env` edits are restricted to an explicit safe allowlist instead of a loose blacklist, and dashboard metrics/report/user slices now filter out synthetic `bridge_*` users consistently in both dashboard and ops trigger helpers
- the April 3 third hardening follow-up is now in code: dangerous dashboard user/payment/support actions now write richer audit snapshots with explicit `before / after` payloads instead of opaque one-line strings, including manual `trial / extend / block / tariff remove / protocol switch`, payment `confirm / reject / status change`, and support `assign / transfer / close`, so operator investigations are no longer blind to the previous state on those seams
- the April 3 fourth hardening follow-up is now in code: finance and server/operator seams now also emit richer `before / after` audit snapshots for `create / approve / cancel / delete finance entry`, `service action`, `create server`, and `server status` transitions, while confirmed revenue payments without a linked `finance_entry` are now picked up by the same periodic confirmed-payment reconcile path instead of staying silently out of the finance ledger forever
- the April 3 hardening follow-up now also tightens dashboard auth and cleanup seams: auth rate limiting applies per username as well as per IP bucket, so rotating IPs no longer bypasses the narrow login/request-code limiter quite as easily, and cookie-auth still rejects explicit cross-site origins on unsafe methods
- dashboard-side user deletion now also removes linked support tickets/messages, finance income rows tied to that user’s payment records, channel touch rows, device-slot entitlements, and recorded activation rows instead of only deleting the user row and leaving obvious local orphans behind
- backend `ensure_schema()` no longer silently marks the schema as ready when `AUTO_APPLY_SCHEMA=0`; it now validates that the required core tables/columns already exist and fails fast if the runtime schema is behind the code
- schema auto-apply is now version-aware instead of blindly replaying every raw `ALTER` on every start: core and dashboard share a `schema_migration_steps` registry, apply only unapplied steps, and record a step as applied only after its effect is actually present
- the April 3 ninth hardening follow-up is now in code: dashboard now stamps every request with an `X-Request-ID`, writes the same `request_id` into audit-log rows automatically through request context, exposes that identifier in serialized audit payloads, and removes several hot unnecessary full-scans by limiting overview audit loading to recent rows, pushing user search filtering into SQL, querying traffic-page audit/support activity only for the last 24 hours, and replacing the overview-wide `VpnRepairEvent` full scan with grouped failed-repair counts only for users that actually need repair attention
- the April 3 tenth hardening follow-up is now in code: dashboard same-origin checks now run from a global HTTP middleware for all unsafe `/dashboard`, `/login`, and `/verify` requests instead of depending only on individual handlers, while synthetic `bridge_/test/debug` rows are additionally filtered out of the shared `v2` users/clients/payments loader, overview support counts, direct synthetic user detail access, and VPN overview device lists so synthetic data no longer leaks back through those legacy/detail seams
- the April 4 dashboard/support/backup follow-up is now in code: support ticket list/search/counts now push search plus synthetic-user filtering down into SQL instead of loading broad slices and trimming them in Python, React Query no longer globally refetches on reconnect by default, restore-readiness falls back to `unknown` unless a machine-readable `restore-validation.json` signal exists, and the current runbook now matches the real role split where `support_admin` is support-only while owner keeps payment/env/delete authority
- the April 4 role-override follow-up is now in code: dashboard settings no longer let owner expand `support_admin` or `tech_admin` beyond their explicit allowlist through runtime permission toggles, so role-permission overrides can narrow or restore only the permissions that belong to that role contract instead of silently reintroducing `manage_users/manage_payments` through the side door
- the April 4 Platega replay follow-up is now in code: exact repeat callbacks with the same payload hash and already-applied product effect now short-circuit as `duplicate` before another provider sync/finalization pass, reducing callback-replay noise on the same confirmed payment without pretending that provider-side cryptographic anti-replay suddenly exists
- the April 3 eleventh hardening follow-up is now in code: region/runtime capabilities are now modeled explicitly instead of treating every VPN surface as if it were still `3x-ui`; Estonia keeps a legacy `provider_type` for historical x-ui-managed profiles where needed, but its current runtime surface is represented as `AmneziaWG` (`awg-quick@awg0`) for watchdog/dashboard runtime labels, service pills, and anti-sharing policy copy, while device technical payloads now expose a dedicated anti-sharing policy summary alongside the existing scope/soft-limit labels
- repo-side backup orchestration is no longer represented only by Windows-local scheduled-task scripts: the repo now ships server-side `ops/backup/core_pg_backup.sh` and `ops/backup/remote_artifact_backup.sh` plus matching `systemd` unit/timer templates, and dashboard backup status can now consume machine-readable status signals from `${DASHBOARD_BACKUP_ROOT}/status/*.json` when they exist
- the April 3 fifth hardening follow-up is now in code: dashboard user deletion now compensates remote-state failures both when a later DB delete step breaks and when one device remote-delete fails after earlier devices were already removed, device provisioning now cleans up newly created local/remote state when metadata persistence or immediate post-create fetch fails before delivery, and `amonora-access-reminders` now purges expired `bridge_*` synthetic users together with their leftover VPN devices instead of leaving bridge accounts/devices to accumulate indefinitely
- the April 3 sixth hardening follow-up is now in code: dashboard-side device creation now also compensates provisioning failures if metadata persistence breaks after the remote client and local row were already created, and dashboard payment/finance slices now exclude synthetic `bridge_/test/debug` payment rows instead of letting those synthetic records leak back into payments lists or finance summaries through linked `payment_record` income rows
- the April 3 seventh hardening follow-up is now in code: dashboard auth now writes explicit audit events for invalid credentials, request-code rate limiting, missing/invalid/overused verification codes, Telegram delivery failures, and successful code verification, while `get_admin_by_session()` now deletes sessions for deactivated admins instead of leaving dead session rows behind
- the April 3 eighth hardening follow-up is now in code: dashboard-side device creation now respects the same Estonia soft-capacity guard as the user bot instead of bypassing it for manual operator-created devices, and dashboard support slices now filter synthetic `bridge_/test/debug` users from ticket lists, support counts, and direct ticket detail views so synthetic support noise no longer leaks back into operator queues
- the April 3 twelfth hardening follow-up is now in code: promo offer copy for `3m / 6m / 12m` is now assembled from one shared helper instead of being duplicated across bot texts and reminder workers, while built-in control-trigger defaults for `trial_ends_* / trial_expired_3d` are now neutral outside the promo window and auto-rewrite older stale promo bodies instead of silently carrying fixed historical dates forward
- the April 3 thirteenth hardening follow-up is now in code: the active `dashboard/ui` now requires explicit operator confirmation before the riskiest user/payment/support mutations (`confirm / reject / status-change payment`, `remove tariff`, `block/unblock user`, `delete device`, `close support ticket`, `delete user`) so common misclick paths no longer execute immediately without an extra intent check
- the April 4 fourteenth hardening follow-up is now in code: the remaining high-impact `dashboard/ui` operator seams now also require explicit confirmation before they fire (`server restart`, `maintenance toggle`, `migration`, `tariff update`, `service restart`, `.env` save, role-permission toggles, admin role/status save, overview repair sync, traffic baseline reset), so the main control-center side effects no longer depend on single-click execution
- the April 4 fifteenth hardening follow-up is now in code: the main `dashboard/ui` polling hooks are now visibility-aware, so notifications/audit/overview/servers/traffic/payments/support stop background auto-refetching while the tab is hidden instead of continuing to hit the API every 20-60 seconds when no operator is actually looking at the panel
- the April 4 dashboard production incident follow-up is now in code: support-ticket real-user filtering no longer sends large Telegram IDs into `users.id IN (...)` when resolving mixed support ticket keys, so `dashboard` `v2/overview` and `v2/notifications` stop throwing asyncpg `value out of int32 range` errors on production accounts with large Telegram IDs
- the April 4 bridge-purge hotfix is now in code: deleting expired synthetic `bridge_*` users now first nulls their links in `control_broadcast_deliveries` and `control_trigger_delivery_logs`, so the active `amonora-access-reminders` worker no longer crashes on older production FK layouts that still block direct `DELETE users`
- the April 4 repair-incident hotfix is now in code and on the core host: Denmark `xray_core` client upserts are idempotent again, so soft sync no longer rewrites the same client and restarts `xray` on every already-correct Denmark device, and dashboard device deletion now treats the retired `EE + xui + admin-visible + reserve-only + dashboard_*` seam as legacy cleanup state instead of trying to reach a non-existent Estonia `xui` runtime during deletion; production repair-needed users dropped back to `0` after replaying users `17`, `18`, and `594`
- the April 4 analytics MVP is now superseded by the April 5 topology: PostgreSQL still carries `analytics_user_attribution`, `analytics_events`, `analytics_daily_stage_counts`, `analytics_daily_revenue`, `analytics_daily_connection`, `analytics_cohort_retention`, and `analytics_refresh_state`; rollups still refresh through `ops/analytics_refresh.py` on core every 10 minutes; but Grafana itself now belongs on Estonia infra-host under `https://grafana.amonoraconnect.com`, with core `/grafana/*` reduced to redirect-only and datasource access going through the Estonia-side DB tunnel instead of a core-side public route
- the April 4 synthetic-source and dashboard query follow-up is now in code: `users` now have an explicit `is_synthetic` DB flag with schema backfill for legacy prefixed accounts, `bridge_*` users created from landing are marked synthetic at write time, shared synthetic helpers and SQL clauses now use that explicit flag instead of depending only on username prefixes, and the dashboard `operations report` / `VPN overview` paths now query only real-user slices first instead of broad full-table loads followed by Python-only filtering
- the April 4 dashboard auth lockout follow-up is now in code: dashboard auth no longer derives lockout only from expensive retrospective audit scans; it now persists explicit per-scope username/IP failure state in `dashboard_auth_lockout_states`, raises durable short-lived lockouts after repeated invalid credentials or verification-code failures, and clears those counters on successful request-code/verify steps while still keeping the older audit-based lockout path as a compatibility fallback
- the April 4 overview-query follow-up is now in code: `dashboard/ui` overview no longer performs two separate `users + vpn_clients + payment_records` reads for the same request, because `get_v2_overview_payload()` now reuses the already-loaded region metrics rows when building `overview_metrics()` instead of forcing a second broad scan of the same real-user slices
- the April 4 payments/search dashboard follow-up is now in code: `get_payment_records()` no longer does a second full user lookup after already filtering synthetic-linked records in SQL, while `v2` payments/search payloads now load only the specific real-user ids referenced by the current payment slice instead of full-scanning the whole `users` table for every payments/search render
- the April 4 env-apply follow-up is now in code: owner-side `.env` edits can optionally run a staged `restart + verify` pass for the affected core services instead of only returning a blind `restart_required` flag, and the settings UI now exposes that explicit `apply now` option with per-service success/failure feedback
- the April 4 support synthetic-filter follow-up is now in code: `support_bot.storage` no longer excludes synthetic tickets only by username prefixes when `exclude_synthetic=True`; it now joins `users` on Telegram ID and prefers the explicit `users.is_synthetic` flag, while keeping the legacy username-prefix rule only as a fallback for orphaned old tickets
- the April 4 payment replay follow-up is now in code: Platega callback handling no longer relies only on the single last seen `hash + status` pair, but keeps a bounded callback-signature history in payment metadata, preserves backward compatibility with older records that only have the legacy hash/status fields, and treats exact replayed terminal callbacks or already-applied confirmed callbacks as cheap no-op duplicates instead of re-entering provider sync/finalization again
- the April 4 payment reconcile follow-up is now in code: confirmed-payment reconcile no longer falls back to loading `all confirmed` rows just to discover access-retry or finance-marker candidates; instead it uses targeted candidate lists for `missing effect`, `applied subscription without successful access sync`, `revenue payment without finance marker`, and `confirmed ids with missing finance entries`, and it now refuses to count a revenue payment as reconciled unless the finance leg also converged, so `payment -> effect -> access -> finance` status is materially more honest for already-applied confirmed rows
- the April 5 payment side-effect follow-up is now in code: subscription/device-slot/balance-topup finalizers now treat `already_applied` and unresolved `in_progress` claims through one canonical idempotent path, refuse to report success when another worker never actually sealed the effect, and stop releasing the payment-effect claim after the irreversible effect has already happened, so duplicate callback/manual/provider retries can no longer reopen the same payment for a second subscription extension, slot grant, or balance credit
- the April 4 device-compensation follow-up is now in code: rare provisioning/delete failure tails no longer stop at `logger.warning`; the repo now has `device_compensation_jobs`, `bot/device_compensation.py`, enqueue points in bot/dashboard device-creation and destructive delete flows, and `amonora-access-reminders` now processes pending cleanup/restore jobs on every run, so failed immediate rollback can still converge later instead of silently leaving local-vs-remote device drift behind
- the April 4 control-trace follow-up is now in code: `control_notification_events` now carry a first-class `request_id`, dashboard-originated control events automatically inherit the same request context id already used by `dashboard_audit_logs`, and control-event payload readers now expose that identifier without breaking older payload-only consumers, so a single admin request can be traced more directly from dashboard action to audit row to control alert
- the April 5 ops query-hardening follow-up is now in code: `amonora-access-reminders` no longer starts several of its loops from unrestricted `select(User)` scans, but instead builds scoped SQL candidate queries for trial-channel enforcement, expiry revocation, trial-device purge, bridge-expiry purge, repair recovery, and trigger delivery; device-count reads are narrowed to the current candidate user ids; and `control_error_triggers` now loads only real `vpn_repair_needed` users plus their device counts at SQL level instead of loading the entire user table and filtering synthetic/healthy rows in Python
- the April 5 support-panel synthetic follow-up is now in code: the Telegram `support_bot` admin panel now requests ticket counts and ticket lists with `exclude_synthetic=True`, so synthetic `bridge_/test/debug` noise is no longer filtered only in web-dashboard surfaces while still leaking into the support-bot operator queue itself
- the April 5 VPN reconcile follow-up is now in code: `ops.vpn_regions.reconcile_vpn_clients()` now distinguishes panel-managed `xui` profiles from current runtime-managed `amneziawg/xray_core` devices instead of counting every non-panel device as “missing”, and the standalone `reconcile_vpn_regions.py` runner now exposes an explicit `--repair-missing-remote` path that can recreate missing `xui` clients for users who still have active access, reusing the same self-healing sync seam instead of treating metadata drift as the only reconcilable class
- the April 5 server-watchdog follow-up is now in code: `ops/server_watchdog.py` no longer loads the entire `users` and `vpn_clients` tables just to find affected users for a node-down incident, but instead joins `vpn_clients -> users` once, filters to real users at SQL level, and computes affected active-access users from that narrower slice, so watchdog notification fanout puts less read pressure on core and no longer treats synthetic users as real outage recipients
- the April 5 dashboard query/index hardening follow-up is now in code: `dashboard` `v2/users` no longer derives device counts, latest payment status, confirmed tariff, and country distribution by loading every matching `vpn_client` and `payment_record` row into Python, but instead uses grouped SQL aggregates and per-user window queries; `VPN overview` and managed-region capacity stats now reuse short-lived runtime cache entries for the default operator view; and both core and dashboard schema registries now carry additional versioned indexes for `vpn_clients`, `payment_records`, `vpn_repair_events`, `support_tickets`, `dashboard_audit_logs`, `dashboard_sessions`, and `dashboard_login_codes`, so the heaviest operator pages rely less on repeated full scans and more on explicit indexed access paths
- the April 5 role/ip/synthetic hardening follow-up is now in code: dashboard legacy and `v2` admin routes no longer rely on broad `role_allows("tech_admin")` priority checks for finance/service/docs/server mutations, but instead require explicit permissions or strict owner-only access; dashboard audit IP resolution now prefers a dedicated trusted reverse-proxy header `X-Amonora-Client-IP` and ignores spoofed forwarded headers from untrusted peers; and `control_bot` audience segmentation now starts from real-user SQL slices instead of full `users` scans, so synthetic `bridge_/test/debug` accounts stop leaking back into broadcast counts, KPI-like operator slices, and campaign recipient building
- the April 5 dashboard/ops noise-hardening follow-up is now in code: all `dashboard/ui` React Query hooks now share explicit quiet defaults for `window focus` and `reconnect`, so even non-polling pages stop silently refetching on tab focus/network bounce; the remaining high-impact server action (`health check`) now uses the same explicit confirm gate as the other node actions; and `amonora-access-reminders` now runs through a named stage pipeline that persists stateful stages after each step, keeps later stages running after an earlier failure, downgrades expected Telegram-delivery failures from warning-noise to informational suppression, and emits one compact structured completion summary instead of a long ad-hoc log line
- the April 5 Platega/audit follow-up is now in code: Platega webhook validation now optionally rejects stale callbacks when the provider payload carries a timestamp, callback replay detection no longer depends only on the raw body hash but also stores a bounded semantic event-history per payment so the same transaction/status/payload cannot re-enter finalization just by arriving with a different raw hash, and existing-record callback invariants now check method/user/tariff/amount/currency before sync and emit a control warning instead of silently trusting conflicting provider data; meanwhile the remaining high-value admin-auth/settings audit seams (`request_login_code`, `logout`, `logout_v2`, `update_notification_preference`) now write structured JSON with actor/session/before-after context instead of empty or plain-string audit rows
- the April 5 env/restore proof follow-up is now in code: owner-side `.env` edits no longer allow silent disk/runtime split-brain because the dashboard path always performs staged `write -> restart -> verify` and automatically rolls the file plus affected services back on failure, while restore-readiness now treats `healthy` as a real machine-readable proof state only when `status/restore-proof.json` carries an explicit `temporary_database_restore` proof payload for `core_pg`; legacy `restore-validation.json` without proof fields now downgrades to `unknown`
- the April 5 Grafana suite follow-up is now superseded by the April 6 strict pass: `ops/grafana/` still owns the repo-managed dashboards, datasource UID, and alert provisioning, but the active suite is now the strict Russian thematic version with compact `Главная Amonora`, `Воронка роста`, `Источники и посты`, `Выручка и монетизация`, `Удержание и отток`, `Качество подключения`, `Операции и ремонты`, and `Алерты и инциденты`
- the same April 5 Grafana suite pass also extends the analytics layer beyond business funnel rollups: PostgreSQL now contains `analytics_hourly_ops_incidents`, `analytics_hourly_ops_snapshots`, and `analytics_runtime_status`, refreshed by `ops/analytics_refresh.py` on the same bounded 10-minute maintenance cycle, so Grafana owner/ops dashboards and alert rules can read repair/incidents/runtime freshness without touching raw operational tables or filesystem state directly
- the April 11 node-health hardening follow-up is now in code: Germany/Denmark server health no longer treats `ssh(22)` latency as the product ping signal, remote probes now prefer the public VPN listener ports (`DE 443`, `DK 443/8443`), `xui_service_status` rather than `xui_status` is the source of truth for xui runtime health in dashboard/watchdog/control alerts, and Germany `3x-ui panel/tunnel` status is kept as a separate control-plane signal so a transient tunnel glitch no longer impersonates a user-facing VPN outage

## Open questions
- which parts of `dashboard` can be marked as truly legacy
- which feature flows are fully migrated to `dashboard/ui`
- which operational assumptions still need production verification
- which test paths are most critical for regression protection
- how backup rotation, dump retention and restore readiness are implemented in practice
- how to turn the first successful core off-host dump copy into a repeatable and governed backup step

## Current priorities
1. Preserve the canonical docs + AI workflow layer
2. Keep runtime truth reconciled into ops docs as the DK/DE/EE node strategy evolves
3. Verify restore-readiness and backup governance
4. Continue repo normalization carefully
5. Keep the near-term focus on core and VPN stabilization

## Product sequencing
- current highest priority: stabilize the core and VPN product
- next adjacent product layer: Automations
- broader ecosystem growth should not override current stabilization needs

## Next 3 actions
1. Decide whether the next highest-value hardening should be `active access without devices`
2. Verify that the new VPN scheduled task runs cleanly on schedule
3. Decide whether restore-drill execution should stay manual-only or later gain a periodic validation cadence

## Blockers
- some runtime/deployment details still require production/server verification
- backup automation and restore readiness are still not fully confirmed end-to-end
