# TASK 124 RESULT — Control center spec refresh

## Result

A new root-level specification file now exists at `amonora_control_tz_v5.md`. It expands the earlier control-center brief into a Russian-first product spec that reflects the clarified user answers about roles, manual SBP review, sync/repair expectations, support flow, node topology, and premium SaaS UI direction.

## What changed

- added a new spec that supersedes the older `v4` brief instead of mutating the earlier file in place;
- separated the scope into a mandatory production core and a UX+ polish layer so completion can be measured honestly;
- captured clarified device/access rules, payment review flow, support lifecycle, node model, audit/alerts, and visual/product requirements;
- updated AI task/state tracking to record that the control-center specification has been refreshed.

## Notes

- this task changes documentation only;
- runtime behavior and deployment were not changed by this step.
