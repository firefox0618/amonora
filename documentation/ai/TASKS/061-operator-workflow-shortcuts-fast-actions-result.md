# TASK 061 — Operator workflow shortcuts (fast actions) Result

## Result
Common operator actions now require fewer navigation hops.

## Added shortcuts
- overview repair cards now expose:
  - `Open user`
  - `Repair VPN`
- overview support attention items now expose:
  - `Open user`
- overview manual payment attention items now expose:
  - `Open user`
- support detail now exposes:
  - `Open user`
  - `Open latest payment`
- payment-linked user context now exposes:
  - `Repair VPN`

## What stayed intentionally simple
- no new pages
- no new backend endpoints
- no batch actions yet
